from googletrans import Translator

# Create a translator object
translator = Translator()

# Get input text from user
input_text = input("Enter the English text to translate to Greek: ")

# Use the translator object to translate the input text
output_text = translator.translate(input_text, dest='el').text

# Print the translated text
print("Translated text: ", output_text)

